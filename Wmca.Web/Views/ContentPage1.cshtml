@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.ContentPageSaferTravel>
@using ContentModels = Wmca.Models.Content;

@{
	Layout = "SaferTravel.cshtml";
}

<main id="ds-main-content" class="ds-container ds-container--main ds-p-t-lg">

    <div class="ds-container">
        <h1>@Model.Name</h1>

        @* Render the block areas *@
        @await Html.GetBlockGridHtmlAsync(Model, "grid")
    </div>

</main>